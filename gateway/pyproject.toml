[project]
name = "skill-gateway"
version = "0.1.0"
description = "FastAPI gateway for Skill-to-Capability."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110,<1.0",
]

[project.optional-dependencies]
dev = [
    "httpx>=0.27,<1.0",
    "pytest>=8,<9",
]
